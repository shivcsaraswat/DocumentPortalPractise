from setuptools import find_packages, setup

setup(
    name = 'documentPortal', 
    author = 'Sunny Savita', 
    version = '0.1',
     ### find_packages identifies potential packages through __init__.py
    packages = find_packages()
)